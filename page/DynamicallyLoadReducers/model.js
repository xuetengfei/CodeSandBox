const initialState = {
  lists: [],
  obj: {},
};

export const namespace = 'xxx';
export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
